<ui:FluentWindow x:Class="Quintilink.Views.SearchResultsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        xmlns:vm="clr-namespace:Quintilink.ViewModels"
        xmlns:converters="clr-namespace:Quintilink.Converters"
        mc:Ignorable="d"
        Title="Search Results" 
        Height="500" 
        Width="700"
        WindowStartupLocation="CenterOwner"
        ExtendsContentIntoTitleBar="True"
        WindowStyle="None"
        d:DataContext="{d:DesignInstance Type=vm:SearchResultsViewModel}">

    <ui:FluentWindow.Resources>
        <converters:StringEmptyConverter x:Key="StringEmptyConverter"/>
    </ui:FluentWindow.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="32"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Title Bar -->
        <ui:TitleBar Grid.Row="0" Title="Search Results" Icon="Search24" />

        <!-- Summary -->
        <ui:Card Grid.Row="1" Margin="15,15,15,10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Orientation="Horizontal">
                    <ui:SymbolIcon Symbol="Search24" FontSize="20" Margin="0,0,10,0" VerticalAlignment="Center"/>
                    <StackPanel>
                        <TextBlock Text="Search Pattern" Foreground="Gray" FontSize="11"/>
                        <TextBlock Text="{Binding SearchPattern}" FontSize="14" FontWeight="SemiBold"/>
                    </StackPanel>
                </StackPanel>

                <StackPanel Grid.Column="2" HorizontalAlignment="Right">
                    <TextBlock Text="Results Found" Foreground="Gray" FontSize="11" TextAlignment="Right"/>
                    <TextBlock Text="{Binding TotalResults}" FontSize="20" FontWeight="Bold" TextAlignment="Right"/>
                </StackPanel>
            </Grid>
        </ui:Card>

        <!-- Results List -->
        <ui:Card Grid.Row="2" Margin="15,0,15,10">
            <ListBox ItemsSource="{Binding Results}" 
                     BorderThickness="0"
                     ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Border BorderBrush="#E0E0E0" 
                                BorderThickness="0,0,0,1" 
                                Padding="10"
                                Margin="-10,0,-10,0">
                            <StackPanel>
                                <!-- Timestamp and Direction -->
                                <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                                    <TextBlock Text="{Binding Timestamp, StringFormat='HH:mm:ss.fff'}" 
                                               FontFamily="Consolas" 
                                               FontSize="11"
                                               Foreground="Gray"
                                               Margin="0,0,10,0"/>
                                    <Border Padding="4,2" 
                                            CornerRadius="3">
                                        <Border.Style>
                                            <Style TargetType="Border">
                                                <Setter Property="Background" Value="#E8F5E9"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding Direction}" Value="TX">
                                                        <Setter Property="Background" Value="#E3F2FD"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding Direction}" Value="ERR">
                                                        <Setter Property="Background" Value="#FFEBEE"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding Direction}" Value="SYS">
                                                        <Setter Property="Background" Value="#F5F5F5"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Border.Style>
                                        <TextBlock Text="{Binding Direction}" 
                                                   FontSize="10" 
                                                   FontWeight="SemiBold"/>
                                    </Border>
                                </StackPanel>

                                <!-- Message -->
                                <TextBlock Text="{Binding Message}" 
                                           FontFamily="Consolas"
                                           FontSize="12"
                                           TextWrapping="Wrap"
                                           Margin="0,0,0,5"
                                           Foreground="#333"/>

                                <!-- Hex Data -->
                                <Border Margin="0,0,0,5" 
                                        Padding="5"
                                        Background="#F5F5F5"
                                        CornerRadius="3"
                                        Visibility="{Binding HexData, Converter={StaticResource StringEmptyConverter}}">
                                    <StackPanel>
                                        <TextBlock Text="HEX:" FontSize="10" Foreground="Gray" Margin="0,0,0,2"/>
                                        <TextBlock Text="{Binding HexData}" 
                                                   FontFamily="Consolas"
                                                   FontSize="11"
                                                   TextWrapping="Wrap"
                                                   Foreground="#0066CC"/>
                                    </StackPanel>
                                </Border>

                                <!-- ASCII Data -->
                                <Border Padding="5"
                                        Background="#F9F9F9"
                                        CornerRadius="3"
                                        Visibility="{Binding AsciiData, Converter={StaticResource StringEmptyConverter}}">
                                    <StackPanel>
                                        <TextBlock Text="ASCII:" FontSize="10" Foreground="Gray" Margin="0,0,0,2"/>
                                        <TextBlock Text="{Binding AsciiData}" 
                                                   FontFamily="Consolas"
                                                   FontSize="11"
                                                   TextWrapping="Wrap"
                                                   Foreground="#666"/>
                                    </StackPanel>
                                </Border>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </ui:Card>

        <!-- Action Buttons -->
        <Border Grid.Row="3" BorderThickness="0,1,0,0" BorderBrush="#E0E0E0" Background="White" Padding="15,10">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Close" Click="CloseButton_Click" MinWidth="80" Padding="10,6"/>
            </StackPanel>
        </Border>
    </Grid>
</ui:FluentWindow>
