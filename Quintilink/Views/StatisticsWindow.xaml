<ui:FluentWindow x:Class="Quintilink.Views.StatisticsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        xmlns:vm="clr-namespace:Quintilink.ViewModels"
        mc:Ignorable="d"
        Title="Connection Statistics" 
        SizeToContent="Height"
        Width="500"
        MinHeight="620"
        WindowStartupLocation="CenterOwner"
        ExtendsContentIntoTitleBar="True"
        WindowStyle="None"
        d:DataContext="{d:DesignInstance Type=vm:StatisticsViewModel}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="32"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <ui:TitleBar Grid.Row="0" Title="Connection Statistics" Icon="DataBarVertical24" />

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="20">
                <ui:Card Margin="0,0,0,15">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0">
                            <TextBlock Text="Connection Status" FontSize="16" FontWeight="SemiBold"/>
                            <TextBlock Text="{Binding ConnectionDuration}" FontSize="22" FontWeight="Bold" Margin="0,4,0,0"/>
                            <TextBlock Text="{Binding QualityStatus, StringFormat=Quality: {0}}" Foreground="Gray" FontSize="12" Margin="0,4,0,0"/>
                            <TextBlock Text="{Binding ErrorCount, StringFormat=Errors: {0:N0}}" Foreground="Gray" FontSize="12"/>
                        </StackPanel>

                        <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                            <ui:Button Command="{Binding CaptureSnapshotCommand}" Margin="0,0,8,0" Height="30" ToolTip="Capture current metrics snapshot">
                                <StackPanel Orientation="Horizontal">
                                    <ui:SymbolIcon Symbol="Save16" FontSize="14" Margin="0,0,4,0"/>
                                    <TextBlock Text="Snapshot"/>
                                </StackPanel>
                            </ui:Button>
                            <ui:Button Command="{Binding ResetStatisticsCommand}" Height="30" ToolTip="Reset current statistics session">
                                <StackPanel Orientation="Horizontal">
                                    <ui:SymbolIcon Symbol="ArrowReset24" FontSize="14" Margin="0,0,4,0"/>
                                    <TextBlock Text="Reset"/>
                                </StackPanel>
                            </ui:Button>
                            <Ellipse Width="14" Height="14" VerticalAlignment="Center" Margin="10,0,0,0">
                                <Ellipse.Style>
                                    <Style TargetType="Ellipse">
                                        <Setter Property="Fill" Value="#D83B01"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsConnected}" Value="True">
                                                <Setter Property="Fill" Value="#107C10"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Ellipse.Style>
                            </Ellipse>
                        </StackPanel>
                    </Grid>
                </ui:Card>

                <ui:Card Margin="0,0,0,15">
                    <StackPanel>
                        <TextBlock Text="Data Transfer" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,10"/>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0" Margin="0,0,10,0">
                                <TextBlock Text="Received" Foreground="Gray" FontSize="12"/>
                                <TextBlock Text="{Binding BytesReceivedReadable}" FontSize="17" FontWeight="Bold" Foreground="#21873a"/>
                                <TextBlock Text="{Binding MessagesReceived, StringFormat='{}{0:N0} messages'}" Foreground="Gray" FontSize="11"/>
                            </StackPanel>
                            <StackPanel Grid.Column="1" Margin="10,0,0,0">
                                <TextBlock Text="Sent" Foreground="Gray" FontSize="12"/>
                                <TextBlock Text="{Binding BytesSentReadable}" FontSize="17" FontWeight="Bold" Foreground="#0451b5"/>
                                <TextBlock Text="{Binding MessagesSent, StringFormat='{}{0:N0} messages'}" Foreground="Gray" FontSize="11"/>
                            </StackPanel>
                        </Grid>
                        <Border BorderThickness="0,1,0,0" BorderBrush="#E0E0E0" Padding="0,10,0,0" Margin="0,10,0,0">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel Grid.Column="0">
                                    <TextBlock Text="Total Bytes" Foreground="Gray" FontSize="11"/>
                                    <TextBlock Text="{Binding TotalBytesReadable}" FontSize="16" FontWeight="Bold"/>
                                </StackPanel>
                                <StackPanel Grid.Column="1">
                                    <TextBlock Text="Total Messages" Foreground="Gray" FontSize="11"/>
                                    <TextBlock Text="{Binding TotalMessages, StringFormat='{}{0:N0} messages'}" FontSize="16" FontWeight="Bold"/>
                                </StackPanel>
                            </Grid>
                        </Border>
                    </StackPanel>
                </ui:Card>

                <ui:Card Margin="0,0,0,15">
                    <StackPanel>
                        <TextBlock Text="Throughput KPIs" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,8"/>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0" Margin="0,0,10,0">
                                <TextBlock Text="Received" Foreground="Gray" FontSize="12"/>
                                <TextBlock Text="{Binding BytesPerSecondReceivedReadable}" FontSize="16" FontWeight="Bold" Foreground="#21873a"/>
                                <TextBlock Text="{Binding PeakBytesPerSecondReceivedReadable, StringFormat='Peak: {0}'}" Foreground="Gray" FontSize="11"/>
                                <TextBlock Text="{Binding BytesPerSecondReceivedReadable, StringFormat='Avg: {0}'}" Foreground="Gray" FontSize="11"/>
                            </StackPanel>
                            <StackPanel Grid.Column="1" Margin="10,0,0,0">
                                <TextBlock Text="Sent" Foreground="Gray" FontSize="12"/>
                                <TextBlock Text="{Binding BytesPerSecondSentReadable}" FontSize="16" FontWeight="Bold" Foreground="#0451b5"/>
                                <TextBlock Text="{Binding PeakBytesPerSecondSentReadable, StringFormat='Peak: {0}'}" Foreground="Gray" FontSize="11"/>
                                <TextBlock Text="{Binding BytesPerSecondSentReadable, StringFormat='Avg: {0}'}" Foreground="Gray" FontSize="11"/>
                            </StackPanel>
                        </Grid>

                        <Grid Margin="0,10,0,0" Height="70" Background="#F6F6F6" ClipToBounds="True">
                            <Canvas>
                                <Polyline Points="{Binding ReceivedTrendPoints}" Stroke="#21873a" StrokeThickness="2"/>
                                <Polyline Points="{Binding SentTrendPoints}" Stroke="#0451b5" StrokeThickness="2"/>
                            </Canvas>
                        </Grid>
                        <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
                            <TextBlock Text="Last 40 samples trend" Foreground="Gray" FontSize="11"/>
                            <TextBlock Text="  •  " Foreground="Gray" FontSize="11"/>
                            <TextBlock Text="RX" Foreground="#21873a" FontSize="11" FontWeight="SemiBold"/>
                            <TextBlock Text=" / " Foreground="Gray" FontSize="11"/>
                            <TextBlock Text="TX" Foreground="#0451b5" FontSize="11" FontWeight="SemiBold"/>
                        </StackPanel>
                    </StackPanel>
                </ui:Card>

                <ui:Card Margin="0,0,0,15">
                    <StackPanel>
                        <TextBlock Text="Average Message Size" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,10"/>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0" Margin="0,0,10,0">
                                <TextBlock Text="Received" Foreground="Gray" FontSize="12"/>
                                <TextBlock Text="{Binding AverageMessageSizeReceivedReadable}" FontSize="16" FontWeight="Bold"/>
                            </StackPanel>
                            <StackPanel Grid.Column="1" Margin="10,0,0,0">
                                <TextBlock Text="Sent" Foreground="Gray" FontSize="12"/>
                                <TextBlock Text="{Binding AverageMessageSizeSentReadable}" FontSize="16" FontWeight="Bold"/>
                            </StackPanel>
                        </Grid>
                    </StackPanel>
                </ui:Card>

                <ui:Card Margin="0,0,0,15">
                    <StackPanel>
                        <TextBlock Text="Session Snapshots" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,10"/>
                        <ListBox ItemsSource="{Binding SessionSnapshots}" MinHeight="80" MaxHeight="140">
                            <ListBox.Style>
                                <Style TargetType="ListBox">
                                    <Setter Property="ToolTip" Value="Saved metric snapshots for this session"/>
                                </Style>
                            </ListBox.Style>
                        </ListBox>
                    </StackPanel>
                </ui:Card>

                <Border BorderBrush="#E0E0E0" BorderThickness="1" Padding="12" Background="#FAFAFA">
                    <Border.Style>
                        <Style TargetType="Border">
                            <Setter Property="Visibility" Value="Visible"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding HasTraffic}" Value="True">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Border.Style>
                    <TextBlock Text="No traffic yet. Send or receive messages to populate throughput charts and KPIs."
                               Foreground="Gray"/>
                </Border>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</ui:FluentWindow>
