<ui:FluentWindow x:Class="Quintilink.Views.AboutWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        xmlns:vm="clr-namespace:Quintilink.ViewModels"
        mc:Ignorable="d"
        Title="About Quintilink" 
        Height="650" 
        Width="450"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        ExtendsContentIntoTitleBar="True"
        WindowStyle="None"
        d:DataContext="{d:DesignInstance Type=vm:AboutViewModel}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="32"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Title Bar -->
        <ui:TitleBar Grid.Row="0" Title="About Quintilink" Icon="Info16" />

        <!-- Content -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="20">
                <!-- App Header -->
                <StackPanel HorizontalAlignment="Center" Margin="0,0,0,20">
                    <ui:SymbolIcon Symbol="Send24" FontSize="48" HorizontalAlignment="Center" Margin="0,0,0,10"/>
                    <TextBlock Text="Quintilink" FontSize="24" FontWeight="Bold" HorizontalAlignment="Center"/>
                    <TextBlock Text="{Binding Version}" FontSize="14" Foreground="Gray" HorizontalAlignment="Center" Margin="0,5,0,0"/>
                </StackPanel>

                <!-- Description -->
                <ui:Card Margin="0,0,0,15">
                    <StackPanel>
                        <TextBlock Text="TCP/Serial Communication Tester" FontSize="14" FontWeight="SemiBold" Margin="0,0,0,5"/>
                        <TextBlock Text="A versatile tool for testing and debugging TCP client/server and serial port communications with support for predefined messages, auto-responses, and hex data visualization." 
                                   TextWrapping="Wrap" Foreground="Gray" FontSize="12"/>
                    </StackPanel>
                </ui:Card>

                <!-- License Info -->
                <ui:Card Margin="0,0,0,15">
                    <StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                            <ui:SymbolIcon Symbol="Shield16" FontSize="16" Margin="0,0,8,0"/>
                            <TextBlock Text="License" FontSize="14" FontWeight="SemiBold"/>
                        </StackPanel>
                        
                        <TextBlock Text="{Binding LicenseSummary}" TextWrapping="Wrap" FontSize="12" Margin="0,0,0,10"/>
                        
                        <TextBlock FontSize="11" Foreground="Gray" TextWrapping="Wrap">
                            <Run Text="For commercial licensing, contact: "/>
                            <Hyperlink NavigateUri="mailto:petr.kurka@gmail.com" RequestNavigate="Hyperlink_RequestNavigate">
                                <Run Text="petr.kurka@gmail.com"/>
                            </Hyperlink>
                        </TextBlock>
                    </StackPanel>
                </ui:Card>

                <!-- Links -->
                <ui:Card Margin="0,0,0,15">
                    <StackPanel>
                        <TextBlock Text="Links" FontSize="14" FontWeight="SemiBold" Margin="0,0,0,8"/>
                        
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,5">
                            <ui:SymbolIcon Symbol="Code16" FontSize="14" Margin="0,0,8,0"/>
                            <TextBlock FontSize="12">
                                <Hyperlink NavigateUri="https://github.com/pekur/Quintilink" RequestNavigate="Hyperlink_RequestNavigate">
                                    <Run Text="GitHub Repository"/>
                                </Hyperlink>
                            </TextBlock>
                        </StackPanel>
                        
                        <StackPanel Orientation="Horizontal">
                            <ui:SymbolIcon Symbol="Document16" FontSize="14" Margin="0,0,8,0"/>
                            <TextBlock FontSize="12">
                                <Hyperlink NavigateUri="https://github.com/pekur/Quintilink/blob/master/LICENSE" RequestNavigate="Hyperlink_RequestNavigate">
                                    <Run Text="Full License Text"/>
                                </Hyperlink>
                            </TextBlock>
                        </StackPanel>
                    </StackPanel>
                </ui:Card>

                <!-- Copyright -->
                <TextBlock Text="{Binding Copyright}" FontSize="11" Foreground="Gray" HorizontalAlignment="Center"/>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</ui:FluentWindow>
