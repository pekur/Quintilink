<ui:FluentWindow x:Class="TcpTester.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        xmlns:vm="clr-namespace:TcpTester.ViewModels"
        xmlns:local="clr-namespace:TcpTester.Behaviors"
        mc:Ignorable="d"
        Title="TCP Tester" Height="500" Width="900"
        Background="{DynamicResource {x:Static SystemColors.ControlBrushKey}}"
        ExtendsContentIntoTitleBar="True"
        WindowStyle="None"
        ResizeMode="CanResize">
    <ui:FluentWindow.DataContext>
        <vm:MainViewModel />
    </ui:FluentWindow.DataContext>

    <ui:FluentWindow.Resources>
    </ui:FluentWindow.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="32"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Custom Fluent title bar -->
        <ui:TitleBar Grid.Row="0" Title="{Binding Title}" Icon="Send24" FontSize="14" FontWeight="SemiBold" />

        <!-- Connection settings -->
        <ui:Card Grid.Column="0" Grid.Row="1" Margin="10,10,10,2">
            <StackPanel Orientation="Horizontal" Margin="-10">
                <CheckBox Content="Server Mode" IsChecked="{Binding IsServerMode}" Margin="0,0,10,0" VerticalAlignment="Center">
                    <CheckBox.Style>
                        <Style TargetType="CheckBox" BasedOn="{StaticResource {x:Type CheckBox}}">
                            <Setter Property="IsEnabled" Value="True"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsConnected}" Value="True">
                                    <Setter Property="IsEnabled" Value="False"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </CheckBox.Style>
                </CheckBox>
                <!-- Host -->
                <TextBox Width="150" Text="{Binding Host}" VerticalAlignment="Center">
                    <TextBox.Style>
                        <Style TargetType="TextBox" BasedOn="{StaticResource {x:Type TextBox}}">
                            <Setter Property="IsEnabled" Value="True"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsServerMode}" Value="True">
                                    <Setter Property="IsEnabled" Value="False"/>
                                </DataTrigger>
                                <DataTrigger Binding="{Binding IsConnected}" Value="True">
                                    <Setter Property="IsEnabled" Value="False"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBox.Style>
                </TextBox>

                <!-- Port -->
                <TextBox Width="70" Text="{Binding Port}" Margin="10,0" VerticalAlignment="Center">
                    <TextBox.Style>
                        <Style TargetType="TextBox" BasedOn="{StaticResource {x:Type TextBox}}">
                            <Setter Property="IsEnabled" Value="True"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsConnected}" Value="True">
                                    <Setter Property="IsEnabled" Value="False"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBox.Style>
                </TextBox>

                <Button Content="Connect" Command="{Binding ConnectCommand}"/>
                <Button Content="Disconnect" Command="{Binding DisconnectCommand}"/>

                <!-- Spacer -->
                <Rectangle Width="20"/>

                <!-- Status indicator -->
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <Ellipse Width="12" Height="12" Margin="0,0,5,0">
                        <Ellipse.Style>
                            <Style TargetType="Ellipse">
                                <Setter Property="Fill" Value="Red"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsConnected}" Value="True">
                                        <Setter Property="Fill" Value="LimeGreen"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Ellipse.Style>
                    </Ellipse>

                    <TextBlock VerticalAlignment="Center" FontWeight="Bold" Text="{Binding ServerStatus}" ToolTip="{Binding ServerStatusTooltip}"/>
                </StackPanel>

            </StackPanel>
        </ui:Card>

        <Grid Grid.Row="2" Background="#F3F3F3" Margin="10,2,10,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="3*"/>
                <ColumnDefinition Width="2*"/>
            </Grid.ColumnDefinitions>

            <!-- Left panel: Log -->
            <ui:Card  Grid.Column="0" VerticalAlignment="Stretch" VerticalContentAlignment="Stretch" Margin="0,0,2,0">
                <TextBox Grid.Row="0" Text="{Binding Log}" AcceptsReturn="True" VerticalScrollBarVisibility="Auto" IsReadOnly="True" TextWrapping="Wrap"
                         Background="#FFFFFF" BorderBrush="#DDD" BorderThickness="1" FontFamily="Consolas" FontSize="13" Margin="-5" local:AutoScrollBehavior.AutoScroll="True" />
            </ui:Card>

            <!-- Right panel: Tabs (icons with tooltips) -->
            <ui:Card Grid.Column="1" VerticalContentAlignment="Stretch" VerticalAlignment="Stretch" Margin="2,0,0,0">
                <TabControl>
                    <!-- Predefined Messages tab -->
                    <TabItem ToolTip="Predefined Messages" Width="32">
                        <TabItem.Header>
                            <ui:SymbolIcon Symbol="TextBox16" FontSize="20"/>
                        </TabItem.Header>

                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <ListBox x:Name="msgList" ItemsSource="{Binding PredefinedMessages}" DisplayMemberPath="Name" Margin="0,0,0,10" MouseDoubleClick="MsgList_MouseDoubleClick" BorderBrush="#DDD" BorderThickness="1" 
                                     VerticalContentAlignment="Stretch" VerticalAlignment="Stretch">
                                <ListBox.ItemContainerStyle>
                                    <Style TargetType="ListBoxItem">
                                        <Setter Property="Padding" Value="2,2"/>
                                        <Setter Property="Margin" Value="0"/>
                                    </Style>
                                </ListBox.ItemContainerStyle>
                            </ListBox>

                            <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right">
                                <ui:Button Command="{Binding SendMessageCommand}" CommandParameter="{Binding SelectedItem, ElementName=msgList}" ToolTip="Send predefined message"
                                           Margin="2,0" Padding="2" Background="Transparent" >
                                    <ui:SymbolIcon Symbol="Send16" FontSize="24"/>
                                </ui:Button>
                                <ui:Button Command="{Binding AddMessageCommand}" ToolTip="Add predefined message"
                                           Margin="2,0" Padding="2" Background="Transparent">
                                    <ui:SymbolIcon Symbol="Add16" FontSize="24"/>
                                </ui:Button>
                                <ui:Button Command="{Binding EditMessageCommand}" CommandParameter="{Binding SelectedItem, ElementName=msgList}" ToolTip="Edit predefined message"
                                           Margin="2,0" Padding="2" Background="Transparent">
                                    <ui:SymbolIcon Symbol="Edit16" FontSize="24"/>
                                </ui:Button>
                                <ui:Button Command="{Binding DeleteMessageCommand}" CommandParameter="{Binding SelectedItem, ElementName=msgList}" ToolTip="Delete predefined message"
                                           Margin="2,0" Padding="2" Background="Transparent">
                                    <ui:SymbolIcon Symbol="Delete16" FontSize="24"/>
                                </ui:Button>
                            </StackPanel>
                        </Grid>
                    </TabItem>

                    <!-- Reactions tab -->
                    <TabItem ToolTip="Reactions" Width="32">
                        <TabItem.Header>
                            <ui:SymbolIcon Symbol="ArrowSwap20" FontSize="20"/>
                        </TabItem.Header>

                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <ListView ItemsSource="{Binding Reactions}" Margin="0,0,0,0" BorderBrush="#DDD" BorderThickness="1" x:Name="reactionsList">
                                <ListView.View>
                                    <GridView>
                                        <GridViewColumn Header="Trigger (HEX)" DisplayMemberBinding="{Binding Key}" Width="140"/>
                                        <GridViewColumn Header="Response Name" DisplayMemberBinding="{Binding Value.Name}" Width="140"/>
                                        <GridViewColumn Header="Response HEX" DisplayMemberBinding="{Binding Value.Content}" Width="200"/>
                                    </GridView>
                                </ListView.View>
                            </ListView>

                            <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,5,0,5" HorizontalAlignment="Right">
                                <ui:Button Command="{Binding AddReactionCommand}" ToolTip="Add reaction"
                                    Margin="2,0" Padding="2" Background="Transparent">
                                    <ui:SymbolIcon Symbol="Add16" FontSize="24"/>
                                </ui:Button>

                                <ui:Button Command="{Binding EditReactionCommand}"
                                    CommandParameter="{Binding SelectedItem, ElementName=reactionsList}"
                                    ToolTip="Edit selected reaction"
                                    Margin="2,0" Padding="2" Background="Transparent">
                                    <ui:SymbolIcon Symbol="Edit16" FontSize="24"/>
                                </ui:Button>

                                <ui:Button Command="{Binding DeleteReactionCommand}"
                                    CommandParameter="{Binding SelectedItem, ElementName=reactionsList}"
                                    ToolTip="Delete selected reaction"
                                    Margin="2,0" Padding="2" Background="Transparent">
                                    <ui:SymbolIcon Symbol="Delete16" FontSize="24"/>
                                </ui:Button>
                            </StackPanel>

                        </Grid>
                    </TabItem>
                </TabControl>
            </ui:Card>

        </Grid>
    </Grid>

</ui:FluentWindow>
